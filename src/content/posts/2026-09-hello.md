---
title: "Hello"
description: "A placeholder post that exists to prove the build pipeline works. Delete it."
pubDate: 2026-09-04
tags: ["meta"]
draft: false
---

This post exists only to exercise the content pipeline: frontmatter validation,
markdown rendering, the post layout, and the index listing. Delete the file once
the first real post is written.

## A heading

Body copy sits in a single column at roughly 68 characters, set in a serif at
generous line height.

- A list item
- Another list item

> A blockquote, for pull quotes and cited passages.

Inline `code` and a [link](https://astro.build) render like this.

```python
# A fenced code block
icer = (cost_new - cost_old) / (qaly_new - qaly_old)
```
